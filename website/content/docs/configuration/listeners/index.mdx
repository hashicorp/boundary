---
layout: docs
page_title: Listeners - configuration reference
description: >-
  The listener stanza configures the addresses and ports on which Boundary will respond to requests.
---

# `listener` stanza

The `listener` stanza configures the addresses and ports on which Boundary will
respond to requests. At this time, there are two listener types:
[TCP](/boundary/docs/configuration/listeners/tcp-listeners) and [Unix](/boundary/docs/configuration/listeners/unix-listeners).


## `listener` purpose

@include '/configuration-reference/listeners/listener-purpose.mdx'