{
  "name": "boundary-docs",
  "description": "Documentation website for Boundary",
  "version": "1.0.0",
  "author": "HashiCorp",
  "scripts": {
    "start": "./scripts/website-preview-start.sh",
    "dev": "npm run start",
    "clean": "rm -rf ./website-preview",
    "generate:readme": "next-hashicorp markdown-blocks README.md",
    "linkcheck": "linkcheck boundary.io",
    "format": "next-hashicorp format content/**/* data/**/*",
    "lint": "next-hashicorp lint content/**/* data/**/*",
    "build": "npm i -g npm@7 && ./scripts/website-preview-build.sh"
  },
  "husky": {
    "hooks": {
      "pre-commit": "next-hashicorp precommit"
    }
  },
  "devDependencies": {
    "@hashicorp/platform-cli": "^1.2.0"
  },
  "dependencies": {
    "next": "^11.1.2"
  },
  "engines": {
    "npm": ">=7.0.0"
  }
}

