build:
  roots:
    - proto/local
    - proto/third_party
    - internal

lint:
  use:
    - DEFAULT
  ignore:
    - protoc-gen-swagger
    - google
    # TODO: Remove once the proto files under oplog are compliant with the linter.
    - oplog